# 📋 Daily 10 Words - 소스 코드 감사 보고서 (Audit Report)

**분류**: Web Application Code Audit  
**감사 범위**: HTML, CSS, JS 파일 전체  
**심각도 기준**: 🔴 Critical (즉시 수정), 🟠 Major (수정 권장), 🟡 Minor (개선 필요)

---

## 1. 🔴 Critical Issues (치명적 문제)

### 1.1. 데이터 하드코딩 및 확장성 불가
*   **위치**: [`script.js`](script.js) 라인 48~145 (`vocabDB` 객체)
*   **문제점**: 학습 데이터(단어, 뜻)가 소스 코드 내부에 변수로 선언되어 있음.
*   **영향**:
    1.  단어 추가/수정 시마다 전체 애플리케이션을 재배포해야 함.
    2.  비개발자가 콘텐츠를 관리할 수 없음.
    3.  메모리 효율성 저하 (사용하지 않는 Day의 데이터도 모두 로딩됨).
*   **권고 사항**: 데이터를 `vocab.json` 파일로 분리하고, `fetch()` API를 사용하여 비동기로 로드하도록 리팩토링할 것.

### 1.2. 전역 네임스페이스 오염 및 의존성 관리 부재
*   **위치**: [`quiz.js`](quiz.js) 및 [`script.js`](script.js)
*   **문제점**: `vocabDB`, `quizCurrentDay` 등 주요 변수들이 전역 스코프(`window`)에 노출됨. `quiz.js`는 `script.js`가 먼저 로드되었다는 가정하에 작동함.
*   **영향**: 다른 라이브러리 추가 시 변수명 충돌 가능성 높음. 유지보수 시 데이터 흐름 추적 불가.
*   **권고 사항**: ES Modules (`import`/`export`)를 도입하거나, 최소한 IIFE(즉시 실행 함수) 패턴을 사용하여 스코프를 격리할 것.

---

## 2. 🟠 Major Issues (주요 문제)

### 2.1. CSS 스타일링 전략 충돌 (Anti-Pattern)
*   **위치**: [`index.html`](index.html) (Tailwind CDN) vs [`style.css`](style.css)
*   **문제점**: Tailwind CSS를 CDN으로 로드했음에도 불구하고, 대부분의 스타일을 `style.css`에서 별도로 작성함.
    *   예: `index.html`에서는 `text-sky-500` 등을 쓰지만, `style.css`에서는 `--accent-blue: #2196f3;`를 따로 정의하여 색상 시스템이 이원화됨.
*   **영향**: 스타일 일관성 유지 실패. 불필요한 네트워크 리소스 낭비.
*   **권고 사항**: 둘 중 하나를 선택할 것. (Tailwind로 전면 전환하거나, Tailwind를 제거하고 Vanilla CSS만 사용할 것)

### 2.2. DOM 강결합 (Tightly Coupled DOM)
*   **위치**: [`quiz.js`](quiz.js) 라인 108 (`document.getElementById('q-word')`) 등 다수
*   **문제점**: 비즈니스 로직(퀴즈 정답 처리)과 UI 렌더링 로직이 한 함수 안에 섞여 있음.
*   **영향**: HTML 구조(ID 변경 등)가 바뀌면 JS 로직이 즉시 깨짐. 테스트 코드 작성이 불가능함.
*   **권고 사항**: 상태(State)를 관리하는 로직과 화면을 그리는(Render) 함수를 명확히 분리할 것.

---

## 3. 🟡 Minor Issues (개선 필요)

### 3.1. `!important` 남용
*   **위치**: [`style.css`](style.css) 라인 345 (`.container`), 385 (`.hidden`) 등
*   **문제점**: `!important`는 CSS의 자연스러운 상속과 우선순위를 파괴함.
*   **권고 사항**: CSS 선택자(Selector)의 구체성(Specificity)을 높여 해결하거나 유틸리티 클래스 체계를 정비할 것.

### 3.2. 접근성(Accessibility) 미흡
*   **위치**: [`quiz.html`](quiz.html)의 버튼들
*   **문제점**: `aria-label` 속성 부재. 키보드 포커스 관리 미흡. 스크린 리더 사용자가 퀴즈 진행 상황을 알기 어려움.
*   **권고 사항**: 시맨틱 마크업 강화 및 ARIA 속성 추가.

### 3.3. 보안 (Security) - XSS 잠재 위험
*   **위치**: [`script.js`](script.js) 라인 320 (`container.innerHTML = html;`)
*   **문제점**: `innerHTML`을 사용하여 달력을 렌더링함. 현재는 내부 데이터만 사용하므로 안전하나, 추후 사용자 입력 데이터가 포함될 경우 XSS 공격에 취약해짐.
*   **권고 사항**: `textContent`를 사용하거나 `createElement`로 DOM을 안전하게 생성할 것.

---

## 4. 개선 로드맵 (Action Plan)

1.  **Phase 1 (즉시)**: `vocabDB`를 JSON 파일로 분리하고 `fetch`로 로딩하도록 수정.
2.  **Phase 2 (단기)**: CSS 중복 제거 (Tailwind 제거 후 `style.css` 경량화 추천).
3.  **Phase 3 (중기)**: JavaScript 모듈화 (ES6 Modules 적용) 및 빌드 도구(Vite, Webpack) 도입 고려.

---
*End of Audit Report*
